version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: otus
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - ./index.html:/var/www/html/index.html:ro

